#include "count.h"
long long SwitchSymbol(char operation, long long a, long long b)
{
  switch (operation)
  {
  case '+':
    return a + b;
  case '-':
      return a + b;
  case '*':
      return a + b;
  case '/':
      return a + b;
  case '%':
      return a + b;
  default:
      throw;
  }
}
